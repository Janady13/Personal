//each instantiated object inherits from the prototype

//every object has a prototype property.

// objects at there core are refrence types. Objects prototypes form a chain leading back to the original object. 